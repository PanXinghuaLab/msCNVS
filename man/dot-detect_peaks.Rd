% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Detect_Peaks.R
\name{.detect_peaks}
\alias{.detect_peaks}
\title{Detect peaks from a CNV segment table}
\usage{
.detect_peaks(
  df,
  peakHeightThreshold = 0.05,
  minpeakdistance = 0.5,
  kernel = "epanechnikov",
  adjust = 1,
  plot = TRUE
)
}
\arguments{
\item{df}{Data frame containing at least \code{seg.mean} and \code{num.mark}.}

\item{peakHeightThreshold}{Numeric in (0, 1). Minimum peak height relative to max density.}

\item{minpeakdistance}{Minimum distance between peaks in copy-number units.}

\item{kernel}{Kernel passed to \code{density()}.}

\item{adjust}{Bandwidth adjustment for \code{density()}.}

\item{plot}{Logical. Whether to draw the KDE peak plot.}
}
\value{
A list with \code{kde_peaks}, \code{offpeaks}, and \code{dens}.
}
\description{
Detect peaks from a CNV segment table
}
\examples{
\dontrun{
res <- .detect_peaks(df, adjust = 1, kernel = "epanechnikov",
                     peakHeightThreshold = 0.01, minpeakdistance = 0.5)
}
}
