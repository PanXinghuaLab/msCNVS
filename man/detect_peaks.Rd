% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-generics.R, R/Detect_Peaks.R
\name{detect_peaks}
\alias{detect_peaks}
\alias{detect_peaks,CNVisionObj-method}
\title{Detect CNV peaks}
\usage{
detect_peaks(
  object,
  peakHeightThreshold = 0.05,
  minpeakdistance = 0.5,
  kernel = "epanechnikov",
  adjust = 1,
  plot = TRUE
)

\S4method{detect_peaks}{CNVisionObj}(
  object,
  peakHeightThreshold = 0.05,
  minpeakdistance = 0.5,
  kernel = "epanechnikov",
  adjust = 1,
  plot = TRUE
)
}
\arguments{
\item{object}{A CNVisionObj with ploidy-scaled segments (after \code{InferPloidy()}).}

\item{peakHeightThreshold}{Numeric in (0, 1). Minimum peak height relative to max density.}

\item{minpeakdistance}{Minimum distance between peaks in copy-number units.}

\item{kernel}{Kernel passed to \code{density()}.}

\item{adjust}{Bandwidth adjustment for \code{density()}.}

\item{plot}{Logical. Whether to draw the KDE peak plot.}
}
\value{
The \code{CNVisionObj} with results stored in \code{object@result}
(\code{kde_peaks}, \code{offpeaks}, \code{dens}, \code{df}).
}
\description{
Identify integer CNV peaks from segmented CNV values using KDE peak finding.
}
\examples{
\dontrun{
obj <- detect_peaks(obj, peakHeightThreshold = 0.05, minpeakdistance = 0.5)
}
}
